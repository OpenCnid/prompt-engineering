# Summary

Rewritten whole each session from the entire record, under ~250 words. Newest
entries keep their detail; older ones collapse toward a label as they age.

---

**2026-08-04 — Audited the prompt-engineering skill against its source, then the
stack around it.** Seven commits here: `SKILL.md` rebuilt at §A, §C, §E, the
master template and practices 1 and 4; `DEVIATIONS.md` added; licence and
`NOTICE` made to survive the install; install command fixed. Five other repos
touched — `upsum` created and published, plus `judge-composition`, `self-play`,
`hypershot-protocol` and `dovetail`.

The curriculum settled provenance: §§A–E compress Parts 4–8, practices 4 and 5
trace to Parts 1 and 9, practices 1 and 3 descend from WonderPrompt, and
practice 6 and the master template are house-authored. The lineage note claimed
to compress the Part 9 capstone and did not; it now says what is true.

Murphy ruled that `...` is a primitive composing inside containers; that a tag
name is a categoric primitive from an open vocabulary; that hypershots are
worked examples carrying form rather than shape; and that a summary runs
newest-detailed to oldest-collapsed.

An A/B run of the skill was void — sub-agents receive the skills listing and can
invoke installed skills unprompted, so every arm reached both versions. Three
defect classes surfaced only as side effects of other work, which is what
`upsum` now exists to catch.

Open: dovetail unpushed (owner's call), and Classes B, C and E of the register.
