# TODO

Every item names what it is blocked on and who can unblock it. An item with no
owner and no blocker is a wish.

- [ ] Push `dovetail`'s `add-upsum-eighth-skill` branch — blocked on Cnid; it is
      the one repository whose remote was not supplied, and it carries the
      eighth-skill wiring plus two submodule pin advances.
- [ ] Push this branch, `d/prompt-engineering-improvements-461d89` — blocked on
      Cnid; 7 commits exist on no remote, and it is the session's primary work.
      Caught by the checks, not by the forward-pass that should have named it.
- [ ] Open PRs for `judge-composition`, `self-play` and `hypershot-protocol` —
      blocked on Cnid; branches are pushed and two PR bodies are drafted.
- [ ] Decide what altitude the prompt-engineering skill is written for — blocked
      on Cnid; it settles about eight Class E items at once and nothing else in
      that class moves until it is answered.
- [ ] Work Classes B and C of `DEVIATIONS.md` — blocked on the altitude question
      above for the items that touch scope; the rest are ready.
- [ ] Fix `better-skill-creator`'s `SKILL.md` at 20,064 characters — blocked on
      nothing; it is the only skill of the eight past the prefix it documents,
      losing its last ~160 characters on every install.
- [ ] Preserve `trellis-engine` — blocked on Cnid; 92 local-only commits, 2
      stashes, 7 uncommitted skill files. Largest unpreserved body of work here.
- [ ] Re-run the skill A/B under real isolation, if the question is still live —
      blocked on nothing; the isolation recipe is verified and recorded in
      `self-play`, and the previous run was void.
